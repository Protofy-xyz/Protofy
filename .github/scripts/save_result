#!/bin/bash

if [ "${{ job.status }}" == "success" ]; then
  echo "${{ matrix.os }}: 🟢" >> ${{ runner.temp }}/result.txt
elif [ "${{ job.status }}" == "failure" ]; then
  echo "${{ matrix.os }}: 🔴" >> ${{ runner.temp }}/result.txt
elif [ "${{ job.status }}" == "cancelled" ]; then
  echo "${{ matrix.os }}: 🟠" >> ${{ runner.temp }}/result.txt
else
  echo "${{ matrix.os }}: ❓" >> ${{ runner.temp }}/result.txt
fi