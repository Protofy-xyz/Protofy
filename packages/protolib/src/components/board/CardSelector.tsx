import { YStack } from "@my/ui"

export const CardSelector = ({ cards, selectedCard, onSelectCard }) => {
    return <YStack height={300} width={600}>
        TODO: widget type selector
    </YStack>
}