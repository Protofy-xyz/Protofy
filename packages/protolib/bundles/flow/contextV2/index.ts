import {flowSwitch} from "./switch";
import {forEach} from "./forEach";
import {filter} from "./filter";
import {map} from "./map";
export default {
    "switch": flowSwitch,
    forEach,
    filter,
    map
}