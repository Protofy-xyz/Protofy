export const getInitialData = (dbPath:string):any[] => {
  //you can return custom initial data depending on the database being created
  //console.log('************************ dbPath: ', dbPath)
  // if(dbPath == '../../data/databases/xxx') return [{
    
  // }]

  return []
}