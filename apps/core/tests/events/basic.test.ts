const path = require('path')
const { v4: uuidv4 } = require('uuid');

describe("Basic tests", () => {

    beforeAll(async () => {

    })
    afterAll(async () => {

    })

    it("Event list should be empty at clean start", async () => {
        expect(false).toBe(false)
    }, 120000)
})
