# Configuring Header Menu Content

In this section we are going to configure the elements of Header Menu right side menu.
![header-menu-content](/documentation/components/headerMenuContent.png)

The elements and its visibility are configure through `packages/app/menu.tsx`.
To modify/add elements of this menu you have modify the previous file. The structure of element is the following:
```javascript
    { label: "Profile", path: "/profile", visibility: session => session.loggedIn }
```
<table>
  <tbody>
    {[
      { property: 'Property', description: 'Description' },
      { property: 'label', description: 'Menu label name' },
      { property: 'path', description: 'Path to redirect when click on element' },
      { property: 'visibility', description: 'Controls the visibility of the item' },
    ].map(item => (
      <tr key={item.property}>
        <td>{item.property}</td>
        <td>{item.description}</td>
      </tr>
    ))}
  </tbody>
</table>
